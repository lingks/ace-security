spring:
    application:
        name: ace-config
    cloud:
      config:
        server:
          native:
            search-locations: classpath:/config
    profiles:
      active: native

server:
    port: 8750 #启动端口